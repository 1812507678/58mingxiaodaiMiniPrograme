<view class="section">
  <swiper indicator-dots="{{indicatorDots}}"
    autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
    <block wx:for="{{imgUrls}}" wx:key="navItems" wx:for-item="item" wx:for-index="itemIdx">
      <swiper-item>
        <image src="{{item}}" class="slide-image" />
      </swiper-item>
    </block>
  </swiper>

  <scroll-view scroll-y class="list" bindscrolltoupper="upper" bindscrolltolower="lower" bindscroll="scroll">

  <block wx:for="{{imgUrls2}}" wx:key="navItems" wx:for-item="item" wx:for-index="itemIdx">
    <view class='section_articleitem'> 
      <image class="article_image" src='http://img02.tooopen.com/images/20150928/tooopen_sy_143912755726.jpg'></image>
    </view>
   
    </block>
  </scroll-view>

</view>
